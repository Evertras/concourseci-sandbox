
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: smeeproxies.smeeproxy.evertras.com
spec:
  group: smeeproxy.evertras.com
  names:
    kind: SmeeProxy
    listKind: SmeeProxyList
    plural: smeeproxies
    singular: smeeproxy
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: SmeeProxy is the Schema for the smeeproxies API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: SmeeProxySpec defines the desired state of SmeeProxy
          properties:
            smeeURL:
              description: SmeeURL points to a smee url like https://smee.io/abcdef
              type: string
            targetURL:
              description: TargetURL is where to point the webhook events to, such
                as http://internal.svc.cluster/hook
              type: string
          required:
          - smeeURL
          - targetURL
          type: object
        status:
          description: SmeeProxyStatus defines the observed state of SmeeProxy
          properties:
            active:
              description: Active is the current pod managing this proxy
              type: string
          required:
          - active
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
